export interface PlannedCompartment {
    id: string,
    name: string,
    depth: number,
    x: number,
    y: number,
    length: number,
    width: number,
    targetLength: number,
    targetWidth: number  
}